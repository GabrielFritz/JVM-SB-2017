#ifndef INSTR_H
#define INSTR_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int (*instr_array[])(execution *p);
int nop(execucao *p);
int aconst_null(execucao *p); 
int iconst_m1(execucao *p); 
int iconst_0(execucao *p); 
int iconst_1(execucao *p); 
int iconst_2(execucao *p); 
int iconst_3(execucao *p); 
int iconst_4(execucao *p); 
int iconst_5(execucao *p); 
int lconst_0(execucao *p); 
int lconst_1(execucao *p); 
int fconst_0(execucao *p); 
int fconst_1(execucao *p); 
int fconst_2(execucao *p); 
int dconst_0(execucao *p); 
int dconst_1(execucao *p); 
int bipush(execucao *p); 
int sipush(execucao *p);
int ldc(execucao *p); 
int ldc_w(execucao *p); 
int ldc2_w(execucao *p); 
int iload(execucao *p); 
int lload(execucao *p); 
int fload(execucao *p); 
int dload(execucao *p); 
int aload(execucao *p); 
int iload_0(execucao *p); 
int iload_1(execucao *p); 
int iload_2(execucao *p); 
int iload_3(execucao *p); 
int lload_0(execucao *p); 
int lload_1(execucao *p); 
int lload_2(execucao *p); 
int lload_3(execucao *p); 
int fload_0(execucao *p); 
int fload_1(execucao *p); 
int fload_2(execucao *p); 
int fload_3(execucao *p); 
int dload_0(execucao *p); 
int dload_1(execucao *p); 
int dload_2(execucao *p); 
int dload_3(execucao *p); 
int aload_0(execucao *p); 
int aload_1(execucao *p); 
int aload_2(execucao *p); 
int aload_3(execucao *p); 
int iaload(execucao *p); 
int laload(execucao *p); 
int faload(execucao *p); 
int daload(execucao *p); 
int aaload(execucao *p); 
int store(execucao *p); 
int store_0(execucao *p); 
int store_1(execucao *p); 
int store_2(execucao *p); 
int store_3(execucao *p); 
int iastore(execucao *p); 
int lastore(execucao *p); 
int fastore(execucao *p); 
int dastore(execucao *p); 
int aastore(execucao *p); 
int pop(execucao *p); 
int pop2(execucao *p); 
int dup(execucao *p); 
int dup_x1(execucao *p); 
int dup_x2(execucao *p); 
int dup2(execucao *p); 
int dup2_x1(execucao *p); 
int dup2_x2(execucao *p); 
int swap(execucao *p);  
int iadd(execucao *p);  
int ladd(execucao *p);  
int fadd(execucao *p);  
int dadd(execucao *p);  
int isub(execucao *p);  
int lsub(execucao *p);  
int fsub(execucao *p);  
int dsub(execucao *p);  
int imul(execucao *p);  
int lmul(execucao *p);  
int fmul(execucao *p);  
int dmul(execucao *p);  
int idiv(execucao *p);  
int ldiv_(execucao *p);  
int fdiv(execucao *p);  
int ddiv(execucao *p);  
int irem(execucao *p);  
int lrem(execucao *p);  
int frem(execucao *p);  
int drem_(execucao *p);  
int ineg(execucao *p);  
int lneg(execucao *p);  
int fneg(execucao *p);  
int dneg(execucao *p);  
int ishl(execucao *p);  
int lshl(execucao *p);  
int ishr(execucao *p);  
int lshr(execucao *p);  
int iushr(execucao *p); 
int lushr(execucao *p); 
int iand(execucao *p);  
int land(execucao *p);  
int ior(execucao *p);  
int lor(execucao *p);  
int ixor(execucao *p);  
int lxor(execucao *p);  
int iinc(execucao *p); 
int i2l(execucao *p);  
int i2f(execucao *p);  
int i2d(execucao *p);  
int l2i(execucao *p);  
int l2f(execucao *p);  
int l2d(execucao *p);  
int f2i(execucao *p);  
int f2l(execucao *p);  
int f2d(execucao *p);  
int d2i(execucao *p);  
int d2l(execucao *p);  
int d2f(execucao *p);  
int i2c(execucao *p);  
int i2s(execucao *p);  
int lcmp(execucao *p);  
int fcmpl(execucao *p);  
int fcmpg(execucao *p);  
int dcmpl(execucao *p);  
int dcmpg(execucao *p);  
int ifeq(execucao *p);  
int ifne(execucao *p);  
int iflt(execucao *p);  
int ifge(execucao *p);  
int ifgt(execucao *p);  
int ifle(execucao *p);  
int if_icmpeq(execucao *p);  
int if_icmpne(execucao *p);  
int if_icmplt(execucao *p);  
int if_icmpge(execucao *p);  
int if_icmpgt(execucao *p);  
int if_icmple(execucao *p);  
int if_acmpeq(execucao *p);  
int if_acmpne(execucao *p);  
int goto_(execucao *p); 
int jsr(execucao *p); 
int ret(execucao *p); 
int tableswitch(execucao *p); 
int lookupswitch(execucao *p); 
int ireturn(execucao *p);  
int lreturn(execucao *p);  
int freturn(execucao *p);  
int dreturn(execucao *p);  
int areturn(execucao *p);  
int return_(execucao *p);  
int getstatic(execucao *p); 
int putstatic(execucao *p); 
int getfield(execucao *p); 
int putfield(execucao *p); 
int invokevirtual(execucao *p); 
int invokespecial(execucao *p); 
int invokestatic(execucao *p); 
int new_(execucao *p); 
int newarray(execucao *p); 
int anewarray(execucao *p); 
int arraylength(execucao *p); 
int multianewarray(execucao *p); 
int ifnull(execucao *p); 
int ifnonnull(execucao *p); 
int goto_w(execucao *p); 
int jsr_w(execucao *p); 
#endif